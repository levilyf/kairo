/**
 * CLI product version, surfaced via `kairo --version` and the startup banner.
 *
 * This is the **public release identity** (`v0.1.0-alpha.1`). Workspace
 * package.json versions intentionally remain `0.1.0` until packages are
 * published to npm; the git tag and CLI banner track the product release.
 */
export const CLI_VERSION = "v0.1.0-alpha.1";
